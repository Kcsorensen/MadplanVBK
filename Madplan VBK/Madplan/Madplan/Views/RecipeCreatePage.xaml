<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:extension="clr-namespace:Madplan.Extensions"
             xmlns:local="clr-namespace:Madplan.Models"
             x:Class="Madplan.Views.RecipeCreatePage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Order="Primary" Text="Tilføj madvare" Clicked="Create_Clicked"/>
    </ContentPage.ToolbarItems>

    <StackLayout>
        <TableView HasUnevenRows="True">
            <TableRoot>
                <TableSection Title="Generelt">
                    <extension:DataEntryCell Label="Navn" Placeholder="Indtast navn" Keyboard="Default"/>
                    <extension:CustomPicker Label="Måltid" ItemSource="{Binding Source={x:Static local:MealType.Current}, Path=ListOfMealTypes}"/>
                </TableSection>
                <TableSection Title="Madvarer"/>
            </TableRoot>
        </TableView>
        <ListView x:Name="listView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

    
</ContentPage>